impl<'a> ::bytesmith::Dissect<'a> for Ip<'a> {
    fn field_tree(&mut self) -> ::bytesmith::FieldNode<'a> {
        Ip::field_tree(self)
    }
    fn handoff(&mut self) -> Option<::bytesmith::Handoff<'a>> {
        Ip::handoff(self)
    }
}

impl<'a> Ip<'a> {
    pub fn parse(data: &'a [u8]) -> Result<(Self, &'a [u8]), ::bytesmith::ParseError> {
        let mut me = Self { data };
        me.version_fatal_check()?;
        me.version_recoverable_check()?;
        me.ihl_fatal_check()?;
        me.ihl_recoverable_check()?;
        me.ttl_fatal_check()?;
        me.ttl_recoverable_check()?;
        me.total_fatal_check()?;
        me.total_recoverable_check()?;
        let len = me.total_end_offset();
        if len.bit != 0 {
            return Err(::bytesmith::ParseError::UnalignedLength(len));
        }
        if data.len() < len.byte {
            return Err(::bytesmith::ParseError::NotEnoughData {
                expected: len.byte,
                got: data.len(),
            });
        }
        Ok((me, &data[len.byte..]))
    }
    pub fn field_tree(&mut self) -> ::bytesmith::FieldNode<'a> {
        let mut children = ::std::vec::Vec::new();
        {
            let me = &mut *self;
            children.push(me.version_present_node());
        }
        {
            let me = &mut *self;
            children.push(me.ihl_present_node());
        }
        {
            let me = &mut *self;
            children.push(me.ttl_present_node());
        }
        {
            let me = &mut *self;
            children.push(me.total_present_node());
        }
        let mut root = ::bytesmith::FieldNode::new(
                "Ip",
                "Ip",
                0usize..self.total_end_offset().bits(),
                ::bytesmith::Value::Struct,
            )
            .with_children(children);
        root.set_paths("");
        root
    }
    pub fn dissect(data: &'a [u8]) -> ::bytesmith::FieldNode<'a> {
        let mut me = Self { data };
        let mut children: ::std::vec::Vec<::bytesmith::FieldNode<'a>> = ::std::vec::Vec::new();
        let mut fatal: Option<::bytesmith::ParseError> = None;
        if fatal.is_none() {
            match me.version_fatal_check() {
                Err(error) => {
                    let start = me.version_start_offset().bits();
                    children
                        .push(
                            ::bytesmith::FieldNode::new(
                                    "version",
                                    "b<4>",
                                    start..start,
                                    ::bytesmith::Value::Opaque,
                                )
                                .with_status(::bytesmith::Status::Error(error)),
                        );
                    fatal = Some(error);
                }
                Ok(()) => {
                    match me.version_recoverable_check() {
                        Err(error) => {
                            let bit_range = me.version_bit_range();
                            children
                                .push(
                                    ::bytesmith::FieldNode::new(
                                            "version",
                                            "b<4>",
                                            bit_range,
                                            ::bytesmith::Value::Opaque,
                                        )
                                        .with_status(::bytesmith::Status::Error(error)),
                                );
                        }
                        Ok(()) => {
                            children.push(me.version_present_node());
                        }
                    }
                }
            }
        }
        if fatal.is_none() {
            match me.ihl_fatal_check() {
                Err(error) => {
                    let start = me.ihl_start_offset().bits();
                    children
                        .push(
                            ::bytesmith::FieldNode::new(
                                    "ihl",
                                    "b<4>",
                                    start..start,
                                    ::bytesmith::Value::Opaque,
                                )
                                .with_status(::bytesmith::Status::Error(error)),
                        );
                    fatal = Some(error);
                }
                Ok(()) => {
                    match me.ihl_recoverable_check() {
                        Err(error) => {
                            let bit_range = me.ihl_bit_range();
                            children
                                .push(
                                    ::bytesmith::FieldNode::new(
                                            "ihl",
                                            "b<4>",
                                            bit_range,
                                            ::bytesmith::Value::Opaque,
                                        )
                                        .with_status(::bytesmith::Status::Error(error)),
                                );
                        }
                        Ok(()) => {
                            children.push(me.ihl_present_node());
                        }
                    }
                }
            }
        }
        if fatal.is_none() {
            match me.ttl_fatal_check() {
                Err(error) => {
                    let start = me.ttl_start_offset().bits();
                    children
                        .push(
                            ::bytesmith::FieldNode::new(
                                    "ttl",
                                    "u8",
                                    start..start,
                                    ::bytesmith::Value::Opaque,
                                )
                                .with_status(::bytesmith::Status::Error(error)),
                        );
                    fatal = Some(error);
                }
                Ok(()) => {
                    match me.ttl_recoverable_check() {
                        Err(error) => {
                            let bit_range = me.ttl_bit_range();
                            children
                                .push(
                                    ::bytesmith::FieldNode::new(
                                            "ttl",
                                            "u8",
                                            bit_range,
                                            ::bytesmith::Value::Opaque,
                                        )
                                        .with_status(::bytesmith::Status::Error(error)),
                                );
                        }
                        Ok(()) => {
                            children.push(me.ttl_present_node());
                        }
                    }
                }
            }
        }
        if fatal.is_none() {
            match me.total_fatal_check() {
                Err(error) => {
                    let start = me.total_start_offset().bits();
                    children
                        .push(
                            ::bytesmith::FieldNode::new(
                                    "total",
                                    "u16",
                                    start..start,
                                    ::bytesmith::Value::Opaque,
                                )
                                .with_status(::bytesmith::Status::Error(error)),
                        );
                    fatal = Some(error);
                }
                Ok(()) => {
                    match me.total_recoverable_check() {
                        Err(error) => {
                            let bit_range = me.total_bit_range();
                            children
                                .push(
                                    ::bytesmith::FieldNode::new(
                                            "total",
                                            "u16",
                                            bit_range,
                                            ::bytesmith::Value::Opaque,
                                        )
                                        .with_status(::bytesmith::Status::Error(error)),
                                );
                        }
                        Ok(()) => {
                            children.push(me.total_present_node());
                        }
                    }
                }
            }
        }
        let root_end = children.last().map(|child| child.bit_range.end).unwrap_or(0);
        let mut root = ::bytesmith::FieldNode::new(
                "Ip",
                "Ip",
                0usize..root_end,
                ::bytesmith::Value::Struct,
            )
            .with_children(children);
        if let Some(error) = fatal {
            root = root.with_status(::bytesmith::Status::Error(error));
        }
        root.set_paths("");
        root
    }
    pub fn handoff(&mut self) -> Option<::bytesmith::Handoff<'a>> {
        None
    }
    #[allow(dead_code)]
    fn version_present_node(&mut self) -> ::bytesmith::FieldNode<'a> {
        let bit_range = self.version_bit_range();
        ::bytesmith::FieldNode::new(
            "version",
            "b<4>",
            bit_range.clone(),
            ::bytesmith::Value::UInt(u128::from(self.version())),
        )
    }
    #[allow(clippy::identity_op)]
    pub fn version(&mut self) -> u8 {
        (self.data[0usize] >> 4usize) & 15u8
    }
    pub fn version_end_offset(&mut self) -> bytesmith::Len {
        bytesmith::Len {
            byte: 0usize,
            bit: 4usize,
        }
    }
    pub fn version_start_offset(&mut self) -> bytesmith::Len {
        bytesmith::Len::ZERO
    }
    pub fn version_bit_range(&mut self) -> ::core::ops::Range<usize> {
        self.version_start_offset().bits()..self.version_end_offset().bits()
    }
    #[allow(dead_code)]
    fn version_fatal_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        {
            let len = self.version_end_offset();
            let expected = len.byte_ceil();
            if self.data.len() < expected {
                return Err(::bytesmith::ParseError::NotEnoughData {
                    expected,
                    got: self.data.len(),
                });
            }
        }
        Ok(())
    }
    #[allow(dead_code)]
    fn version_recoverable_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        Ok(())
    }
    #[allow(dead_code)]
    fn ihl_present_node(&mut self) -> ::bytesmith::FieldNode<'a> {
        let bit_range = self.ihl_bit_range();
        ::bytesmith::FieldNode::new(
            "ihl",
            "b<4>",
            bit_range.clone(),
            ::bytesmith::Value::UInt(u128::from(self.ihl())),
        )
    }
    #[allow(clippy::identity_op)]
    pub fn ihl(&mut self) -> u8 {
        (self.data[0usize] >> 0usize) & 15u8
    }
    pub fn ihl_end_offset(&mut self) -> bytesmith::Len {
        bytesmith::Len {
            byte: 1usize,
            bit: 0usize,
        }
    }
    pub fn ihl_start_offset(&mut self) -> bytesmith::Len {
        self.version_end_offset()
    }
    pub fn ihl_bit_range(&mut self) -> ::core::ops::Range<usize> {
        self.ihl_start_offset().bits()..self.ihl_end_offset().bits()
    }
    #[allow(dead_code)]
    fn ihl_fatal_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        {
            let len = self.ihl_end_offset();
            let expected = len.byte_ceil();
            if self.data.len() < expected {
                return Err(::bytesmith::ParseError::NotEnoughData {
                    expected,
                    got: self.data.len(),
                });
            }
        }
        Ok(())
    }
    #[allow(dead_code)]
    fn ihl_recoverable_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        Ok(())
    }
    #[allow(dead_code)]
    fn ttl_present_node(&mut self) -> ::bytesmith::FieldNode<'a> {
        let bit_range = self.ttl_bit_range();
        ::bytesmith::FieldNode::new(
            "ttl",
            "u8",
            bit_range.clone(),
            ::bytesmith::Value::UInt(u128::from(self.ttl())),
        )
    }
    #[allow(clippy::identity_op)]
    pub fn ttl(&mut self) -> u8 {
        self.data[1usize]
    }
    pub fn ttl_end_offset(&mut self) -> bytesmith::Len {
        bytesmith::Len {
            byte: 2usize,
            bit: 0usize,
        }
    }
    pub fn ttl_start_offset(&mut self) -> bytesmith::Len {
        self.ihl_end_offset()
    }
    pub fn ttl_bit_range(&mut self) -> ::core::ops::Range<usize> {
        self.ttl_start_offset().bits()..self.ttl_end_offset().bits()
    }
    #[allow(dead_code)]
    fn ttl_fatal_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        {
            let len = self.ttl_end_offset();
            let expected = len.byte_ceil();
            if self.data.len() < expected {
                return Err(::bytesmith::ParseError::NotEnoughData {
                    expected,
                    got: self.data.len(),
                });
            }
        }
        Ok(())
    }
    #[allow(dead_code)]
    fn ttl_recoverable_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        Ok(())
    }
    #[allow(dead_code)]
    fn total_present_node(&mut self) -> ::bytesmith::FieldNode<'a> {
        let bit_range = self.total_bit_range();
        ::bytesmith::FieldNode::new(
            "total",
            "u16",
            bit_range.clone(),
            ::bytesmith::Value::UInt(u128::from(self.total())),
        )
    }
    #[allow(clippy::identity_op)]
    pub fn total(&mut self) -> u16 {
        u16::from_be_bytes(self.data[2usize..4usize].try_into().unwrap())
    }
    pub fn total_end_offset(&mut self) -> bytesmith::Len {
        bytesmith::Len {
            byte: 4usize,
            bit: 0usize,
        }
    }
    pub fn total_start_offset(&mut self) -> bytesmith::Len {
        self.ttl_end_offset()
    }
    pub fn total_bit_range(&mut self) -> ::core::ops::Range<usize> {
        self.total_start_offset().bits()..self.total_end_offset().bits()
    }
    #[allow(dead_code)]
    fn total_fatal_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        {
            let len = self.total_end_offset();
            let expected = len.byte_ceil();
            if self.data.len() < expected {
                return Err(::bytesmith::ParseError::NotEnoughData {
                    expected,
                    got: self.data.len(),
                });
            }
        }
        Ok(())
    }
    #[allow(dead_code)]
    fn total_recoverable_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        Ok(())
    }
}

impl<'a> IpWriter<'a> {
    pub const SIZE: usize = 4usize;
    pub fn new(data: &'a mut [u8]) -> ::bytesmith::WriteResult<Self> {
        if data.len() < Self::SIZE {
            return Err(::bytesmith::WriteError::NotEnoughSpace {
                expected: Self::SIZE,
                got: data.len(),
            });
        }
        Ok(Self { data })
    }
    pub fn writer_over(data: &'a mut [u8]) -> ::bytesmith::WriteResult<Self> {
        if data.len() < Self::SIZE {
            return Err(::bytesmith::WriteError::NotEnoughSpace {
                expected: Self::SIZE,
                got: data.len(),
            });
        }
        Ok(Self { data })
    }
    pub fn set_version(&mut self, value: u8) -> &mut Self {
        self.data[0usize] = (self.data[0usize] & 15u8) | ((value & 15u8) << 4usize);
        self
    }
    pub fn set_ihl(&mut self, value: u8) -> &mut Self {
        self.data[0usize] = (self.data[0usize] & 240u8) | ((value & 15u8) << 0usize);
        self
    }
    pub fn set_ttl(&mut self, value: u8) -> &mut Self {
        self.data[1usize] = value;
        self
    }
    pub fn set_total(&mut self, value: u16) -> &mut Self {
        self.data[2usize..4usize].copy_from_slice(&value.to_be_bytes());
        self
    }
    pub fn write_into(
        data: &'a mut [u8],
        content: &IpContent,
    ) -> ::bytesmith::WriteResult<usize> {
        let mut w = Self::new(data)?;
        w.set_version(content.version);
        w.set_ihl(content.ihl);
        w.set_ttl(content.ttl);
        w.set_total(content.total);
        Ok(Self::SIZE)
    }
    pub fn to_vec(content: &IpContent) -> ::std::vec::Vec<u8> {
        let mut buf = ::std::vec![0u8; Self::SIZE];
        let _ = IpWriter::write_into(&mut buf, content);
        buf
    }
}

pub struct Ip<'a> {
    #[allow(dead_code)]
    data: &'a [u8],
}

pub struct IpContent {
    pub version: u8,
    pub ihl: u8,
    pub ttl: u8,
    pub total: u16,
}

pub struct IpWriter<'a> {
    data: &'a mut [u8],
}

