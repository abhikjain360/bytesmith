#[allow(non_camel_case_types)]
pub struct Eth_dst_Iterator<'a> {
    idx: usize,
    count: usize,
    data: &'a [u8],
}

#[allow(non_camel_case_types)]
pub struct Eth_src_Iterator<'a> {
    idx: usize,
    count: usize,
    data: &'a [u8],
}

impl<'a> ::bytesmith::Dissect<'a> for Eth<'a> {
    fn field_tree(&mut self) -> ::bytesmith::FieldNode<'a> {
        Eth::field_tree(self)
    }
    fn handoff(&mut self) -> Option<::bytesmith::Handoff<'a>> {
        Eth::handoff(self)
    }
}

impl<'a> ::std::iter::Iterator for Eth_dst_Iterator<'a> {
    type Item = ::bytesmith::ParseResult<u8>;
    fn next(&mut self) -> std::option::Option<Self::Item> {
        if self.idx == self.count {
            return None;
        }
        self.idx += 1;
        let value = self.data[0];
        self.data = &self.data[1..];
        Some(Ok(value))
    }
}

impl<'a> ::std::iter::Iterator for Eth_src_Iterator<'a> {
    type Item = ::bytesmith::ParseResult<u8>;
    fn next(&mut self) -> std::option::Option<Self::Item> {
        if self.idx == self.count {
            return None;
        }
        self.idx += 1;
        let value = self.data[0];
        self.data = &self.data[1..];
        Some(Ok(value))
    }
}

impl<'a> Eth<'a> {
    pub fn parse(data: &'a [u8]) -> Result<(Self, &'a [u8]), ::bytesmith::ParseError> {
        let mut me = Self { data };
        me.dst_fatal_check()?;
        me.dst_recoverable_check()?;
        me.src_fatal_check()?;
        me.src_recoverable_check()?;
        me.ethertype_fatal_check()?;
        me.ethertype_recoverable_check()?;
        let len = me.ethertype_end_offset();
        if len.bit != 0 {
            return Err(::bytesmith::ParseError::UnalignedLength(len));
        }
        if data.len() < len.byte {
            return Err(::bytesmith::ParseError::NotEnoughData {
                expected: len.byte,
                got: data.len(),
            });
        }
        Ok((me, &data[len.byte..]))
    }
    pub fn field_tree(&mut self) -> ::bytesmith::FieldNode<'a> {
        let mut children = ::std::vec::Vec::new();
        {
            let me = &mut *self;
            children.push(me.dst_present_node());
        }
        {
            let me = &mut *self;
            children.push(me.src_present_node());
        }
        {
            let me = &mut *self;
            children.push(me.ethertype_present_node());
        }
        let mut root = ::bytesmith::FieldNode::new(
                "Eth",
                "Eth",
                0usize..self.ethertype_end_offset().bits(),
                ::bytesmith::Value::Struct,
            )
            .with_children(children);
        root.set_paths("");
        root
    }
    pub fn dissect(data: &'a [u8]) -> ::bytesmith::FieldNode<'a> {
        let mut me = Self { data };
        let mut children: ::std::vec::Vec<::bytesmith::FieldNode<'a>> = ::std::vec::Vec::new();
        let mut fatal: Option<::bytesmith::ParseError> = None;
        if fatal.is_none() {
            match me.dst_fatal_check() {
                Err(error) => {
                    let start = me.dst_start_offset().bits();
                    children
                        .push(
                            ::bytesmith::FieldNode::new(
                                    "dst",
                                    "[u8]",
                                    start..start,
                                    ::bytesmith::Value::Opaque,
                                )
                                .with_status(::bytesmith::Status::Error(error)),
                        );
                    fatal = Some(error);
                }
                Ok(()) => {
                    match me.dst_recoverable_check() {
                        Err(error) => {
                            let bit_range = me.dst_bit_range();
                            children
                                .push(
                                    ::bytesmith::FieldNode::new(
                                            "dst",
                                            "[u8]",
                                            bit_range,
                                            ::bytesmith::Value::Opaque,
                                        )
                                        .with_status(::bytesmith::Status::Error(error)),
                                );
                        }
                        Ok(()) => {
                            children.push(me.dst_present_node());
                        }
                    }
                }
            }
        }
        if fatal.is_none() {
            match me.src_fatal_check() {
                Err(error) => {
                    let start = me.src_start_offset().bits();
                    children
                        .push(
                            ::bytesmith::FieldNode::new(
                                    "src",
                                    "[u8]",
                                    start..start,
                                    ::bytesmith::Value::Opaque,
                                )
                                .with_status(::bytesmith::Status::Error(error)),
                        );
                    fatal = Some(error);
                }
                Ok(()) => {
                    match me.src_recoverable_check() {
                        Err(error) => {
                            let bit_range = me.src_bit_range();
                            children
                                .push(
                                    ::bytesmith::FieldNode::new(
                                            "src",
                                            "[u8]",
                                            bit_range,
                                            ::bytesmith::Value::Opaque,
                                        )
                                        .with_status(::bytesmith::Status::Error(error)),
                                );
                        }
                        Ok(()) => {
                            children.push(me.src_present_node());
                        }
                    }
                }
            }
        }
        if fatal.is_none() {
            match me.ethertype_fatal_check() {
                Err(error) => {
                    let start = me.ethertype_start_offset().bits();
                    children
                        .push(
                            ::bytesmith::FieldNode::new(
                                    "ethertype",
                                    "u16",
                                    start..start,
                                    ::bytesmith::Value::Opaque,
                                )
                                .with_status(::bytesmith::Status::Error(error)),
                        );
                    fatal = Some(error);
                }
                Ok(()) => {
                    match me.ethertype_recoverable_check() {
                        Err(error) => {
                            let bit_range = me.ethertype_bit_range();
                            children
                                .push(
                                    ::bytesmith::FieldNode::new(
                                            "ethertype",
                                            "u16",
                                            bit_range,
                                            ::bytesmith::Value::Opaque,
                                        )
                                        .with_status(::bytesmith::Status::Error(error)),
                                );
                        }
                        Ok(()) => {
                            children.push(me.ethertype_present_node());
                        }
                    }
                }
            }
        }
        let root_end = children.last().map(|child| child.bit_range.end).unwrap_or(0);
        let mut root = ::bytesmith::FieldNode::new(
                "Eth",
                "Eth",
                0usize..root_end,
                ::bytesmith::Value::Struct,
            )
            .with_children(children);
        if let Some(error) = fatal {
            root = root.with_status(::bytesmith::Status::Error(error));
        }
        root.set_paths("");
        root
    }
    pub fn handoff(&mut self) -> Option<::bytesmith::Handoff<'a>> {
        None
    }
    #[allow(dead_code)]
    fn dst_present_node(&mut self) -> ::bytesmith::FieldNode<'a> {
        let bit_range = self.dst_bit_range();
        {
            let mut elem_nodes = ::std::vec::Vec::new();
            if let Ok(iter) = self.dst() {
                let mut start = bit_range.start;
                for (i, elem) in iter.enumerate() {
                    let end = start.saturating_add(8usize);
                    match elem {
                        Ok(value) => {
                            elem_nodes
                                .push(
                                    ::bytesmith::FieldNode::new(
                                        i.to_string(),
                                        "u8",
                                        start..end,
                                        ::bytesmith::Value::UInt(u128::from(value)),
                                    ),
                                )
                        }
                        Err(error) => {
                            elem_nodes
                                .push(
                                    ::bytesmith::FieldNode::new(
                                            i.to_string(),
                                            "u8",
                                            start..start,
                                            ::bytesmith::Value::Opaque,
                                        )
                                        .with_status(::bytesmith::Status::Error(error)),
                                );
                        }
                    }
                    start = end;
                }
            }
            ::bytesmith::FieldNode::new(
                    "dst",
                    "[u8]",
                    bit_range.clone(),
                    ::bytesmith::Value::Array,
                )
                .with_children(elem_nodes)
        }
    }
    #[allow(clippy::identity_op)]
    pub fn dst(&mut self) -> ::bytesmith::ParseResult<Eth_dst_Iterator<'a>> {
        Ok(Eth_dst_Iterator {
            idx: 0,
            count: 6usize,
            data: &self.data[0usize..],
        })
    }
    pub fn dst_end_offset(&mut self) -> bytesmith::Len {
        bytesmith::Len {
            byte: 6usize,
            bit: 0usize,
        }
    }
    pub fn dst_start_offset(&mut self) -> bytesmith::Len {
        bytesmith::Len::ZERO
    }
    pub fn dst_bit_range(&mut self) -> ::core::ops::Range<usize> {
        self.dst_start_offset().bits()..self.dst_end_offset().bits()
    }
    #[allow(dead_code)]
    fn dst_fatal_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        {
            let len = self.dst_end_offset();
            let expected = len.byte_ceil();
            if self.data.len() < expected {
                return Err(::bytesmith::ParseError::NotEnoughData {
                    expected,
                    got: self.data.len(),
                });
            }
        }
        Ok(())
    }
    #[allow(dead_code)]
    fn dst_recoverable_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        Ok(())
    }
    #[allow(dead_code)]
    fn src_present_node(&mut self) -> ::bytesmith::FieldNode<'a> {
        let bit_range = self.src_bit_range();
        {
            let mut elem_nodes = ::std::vec::Vec::new();
            if let Ok(iter) = self.src() {
                let mut start = bit_range.start;
                for (i, elem) in iter.enumerate() {
                    let end = start.saturating_add(8usize);
                    match elem {
                        Ok(value) => {
                            elem_nodes
                                .push(
                                    ::bytesmith::FieldNode::new(
                                        i.to_string(),
                                        "u8",
                                        start..end,
                                        ::bytesmith::Value::UInt(u128::from(value)),
                                    ),
                                )
                        }
                        Err(error) => {
                            elem_nodes
                                .push(
                                    ::bytesmith::FieldNode::new(
                                            i.to_string(),
                                            "u8",
                                            start..start,
                                            ::bytesmith::Value::Opaque,
                                        )
                                        .with_status(::bytesmith::Status::Error(error)),
                                );
                        }
                    }
                    start = end;
                }
            }
            ::bytesmith::FieldNode::new(
                    "src",
                    "[u8]",
                    bit_range.clone(),
                    ::bytesmith::Value::Array,
                )
                .with_children(elem_nodes)
        }
    }
    #[allow(clippy::identity_op)]
    pub fn src(&mut self) -> ::bytesmith::ParseResult<Eth_src_Iterator<'a>> {
        Ok(Eth_src_Iterator {
            idx: 0,
            count: 6usize,
            data: &self.data[6usize..],
        })
    }
    pub fn src_end_offset(&mut self) -> bytesmith::Len {
        bytesmith::Len {
            byte: 12usize,
            bit: 0usize,
        }
    }
    pub fn src_start_offset(&mut self) -> bytesmith::Len {
        self.dst_end_offset()
    }
    pub fn src_bit_range(&mut self) -> ::core::ops::Range<usize> {
        self.src_start_offset().bits()..self.src_end_offset().bits()
    }
    #[allow(dead_code)]
    fn src_fatal_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        {
            let len = self.src_end_offset();
            let expected = len.byte_ceil();
            if self.data.len() < expected {
                return Err(::bytesmith::ParseError::NotEnoughData {
                    expected,
                    got: self.data.len(),
                });
            }
        }
        Ok(())
    }
    #[allow(dead_code)]
    fn src_recoverable_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        Ok(())
    }
    #[allow(dead_code)]
    fn ethertype_present_node(&mut self) -> ::bytesmith::FieldNode<'a> {
        let bit_range = self.ethertype_bit_range();
        ::bytesmith::FieldNode::new(
            "ethertype",
            "u16",
            bit_range.clone(),
            ::bytesmith::Value::UInt(u128::from(self.ethertype())),
        )
    }
    #[allow(clippy::identity_op)]
    pub fn ethertype(&mut self) -> u16 {
        u16::from_be_bytes(self.data[12usize..14usize].try_into().unwrap())
    }
    pub fn ethertype_end_offset(&mut self) -> bytesmith::Len {
        bytesmith::Len {
            byte: 14usize,
            bit: 0usize,
        }
    }
    pub fn ethertype_start_offset(&mut self) -> bytesmith::Len {
        self.src_end_offset()
    }
    pub fn ethertype_bit_range(&mut self) -> ::core::ops::Range<usize> {
        self.ethertype_start_offset().bits()..self.ethertype_end_offset().bits()
    }
    #[allow(dead_code)]
    fn ethertype_fatal_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        {
            let len = self.ethertype_end_offset();
            let expected = len.byte_ceil();
            if self.data.len() < expected {
                return Err(::bytesmith::ParseError::NotEnoughData {
                    expected,
                    got: self.data.len(),
                });
            }
        }
        Ok(())
    }
    #[allow(dead_code)]
    fn ethertype_recoverable_check(&mut self) -> Result<(), ::bytesmith::ParseError> {
        Ok(())
    }
}

impl<'a> EthWriter<'a> {
    pub const SIZE: usize = 14usize;
    pub fn new(data: &'a mut [u8]) -> ::bytesmith::WriteResult<Self> {
        if data.len() < Self::SIZE {
            return Err(::bytesmith::WriteError::NotEnoughSpace {
                expected: Self::SIZE,
                got: data.len(),
            });
        }
        Ok(Self { data })
    }
    pub fn writer_over(data: &'a mut [u8]) -> ::bytesmith::WriteResult<Self> {
        if data.len() < Self::SIZE {
            return Err(::bytesmith::WriteError::NotEnoughSpace {
                expected: Self::SIZE,
                got: data.len(),
            });
        }
        Ok(Self { data })
    }
    pub fn dst_mut(&mut self) -> &mut [u8] {
        &mut self.data[0usize..6usize]
    }
    pub fn src_mut(&mut self) -> &mut [u8] {
        &mut self.data[6usize..12usize]
    }
    pub fn set_ethertype(&mut self, value: u16) -> &mut Self {
        self.data[12usize..14usize].copy_from_slice(&value.to_be_bytes());
        self
    }
    pub fn write_into(
        data: &'a mut [u8],
        content: &EthContent,
    ) -> ::bytesmith::WriteResult<usize> {
        let mut w = Self::new(data)?;
        w.dst_mut().copy_from_slice(&content.dst);
        w.src_mut().copy_from_slice(&content.src);
        w.set_ethertype(content.ethertype);
        Ok(Self::SIZE)
    }
    pub fn to_vec(content: &EthContent) -> ::std::vec::Vec<u8> {
        let mut buf = ::std::vec![0u8; Self::SIZE];
        let _ = EthWriter::write_into(&mut buf, content);
        buf
    }
}

pub struct Eth<'a> {
    #[allow(dead_code)]
    data: &'a [u8],
}

pub struct EthContent {
    pub dst: [u8; 6usize],
    pub src: [u8; 6usize],
    pub ethertype: u16,
}

pub struct EthWriter<'a> {
    data: &'a mut [u8],
}

